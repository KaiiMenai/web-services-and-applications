# Assignments - Web Services and Applications

author: Kyra Menai Hamilton

This folder will contain work for the Web Services and Applications module assignments.

## assignment2-carddraw.ipynb

The program outlined in this notebook uses the [Deck of Cards API](https://deckofcardsapi.com/) to simulate [shuffling](https://deckofcardsapi.com/api/deck/new/shuffle/?deck_count=1) and [drawing](https://deckofcardsapi.com/api/deck/<<deck_id>>/draw/?count=2) cards from a deck.

Import required list:

- `import requests`
- `from collections import Counter`

## assignment03-cso.ipynb

The program outlined in this notebook retrieves the dataset for the *exchequer account (historical series)* from the CSO , and stores it into a file called *cso.json*.

Import required list:

- `import requests`
- `import json`

## assignment04-github.py

This program modifies the contents of *assignment04-github.txt* then commits and pushes the changes up to github directly.

Import required list:

- `from pathlib import Path`
- `import subprocess`
- `import sys`

# END